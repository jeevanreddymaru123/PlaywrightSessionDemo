pipeline {
    agent any
    stages {
        stage('Install Dependencies') {
            steps {
                bat 'npm ci'
                bat 'npx playwright install --with-deps'
            }
        }
        stage('Run all cucumber tests') {
            steps {
                bat 'npm run test'
            }
        }        
    }
}